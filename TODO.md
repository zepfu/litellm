# TODO

## In Progress

- Finish the NVIDIA Anthropic-adapter lane and validation work.
  Current target: direct `nvidia/...` agent models from `~/.claude/agents` should route through a dedicated `anthropic_nvidia_completion_adapter` lane with `nvidia` egress-family enforcement, OpenAI-compatible NVIDIA logging support, and `nvidia_nim/...` cost-map coverage. The end state should match the existing adapted providers: `session_history` rows with the normalized upstream model and non-zero cost when pricing is mapped, `session_history_tool_activity` rows when tool/agent dispatch occurs, Langfuse trace environment matching the selected target, tags / metadata / spans including `route:anthropic_nvidia_completion_adapter`, `anthropic-nvidia-completion-adapter`, `anthropic-adapter-target:nvidia:/v1/chat/completions`, and the `anthropic.nvidia_completion_adapter` span name. If NVIDIA does not expose usable non-free pricing for a target model, use the closest equivalent OpenRouter pricing as the fallback basis rather than leaving long-term cost tracking unmapped. DeepSeek and GLM now pass focused prod validation on `aawm.34`; MiniMax remains the known high-latency opt-in path and should stay out of the default suite unless explicitly requested.

- Keep Codex/OpenAI streaming tool activity aligned across Langfuse and `session_history`.
  Current target: `response.output_item.*` / `response.function_call_arguments.*` reconstruction should continue to yield `usage_tool_call_count`, `codex_tool_call_count`, and `session_history_tool_activity` rows for Claude-to-Codex tool runs on `:4001`.

- Preserve tenant identity into `public.session_history` for every passthrough and Anthropic-adapter lane.
  Current target: derive the tenant id once from the canonical request/auth/session metadata source, preserve it through Langfuse trace/session metadata, and persist it into the corresponding `public.session_history` row for native Anthropic, native OpenAI/Codex, native Gemini, and Claude `/anthropic` adapter traffic routed to OpenAI, Google/Gemini, OpenRouter, and NVIDIA. The value should not be inferred from model names or provider lanes, and it should survive streaming, non-streaming, tool-call, cache, and reasoning-effort paths. Unit coverage should feed normalized passthrough/adapted kwargs through `AawmAgentIdentity` / `_build_session_history_record()` and assert tenant id preservation; harness coverage should query `public.session_history` for the controlled harness session id and assert the expected tenant id is populated alongside Langfuse session/user/environment metadata.

- Keep the adapter harness aligned with the real stored session shapes on both `:4001` dev and `:4000` prod targets.
  Current target: `claude_adapter_codex_tool_activity` must hard-gate the `Bash` / `pwd` persistence path, `claude_adapter_ctx_marker` must keep validating `:#port-allocation.ctx#:` via the rewritten request body instead of a brittle exact model reply, `claude_adapter_ctx_marker_escaped` must keep validating `\\:#name.ctx#\\:` literal escaping, dispatched child-agent backtick and bare-acronym lookups should stay aligned with the same `tristore_search_exact` semantics, the CommonMark system-prompt identifier list rewrite should stay aligned with the tenant/agent-scoped raw-content query until the stored procedure lands, the provider-cache canary should keep finding at least one Anthropic `hit` / `write` row in the default suite, Gemini fanout should keep validating the parent session’s delegated `Agent` rows without assuming every Gemini child emits its own command activity, and `--target dev` / `--target prod` should continue enforcing the correct port, Docker container, and Langfuse trace environment. Claude trace-user validation must inject a harness-controlled `x-litellm-end-user-id` / `langfuse_trace_user_id` value via the explicit per-run Claude `--settings` overlay and validate that exact value without hard-coding an ambient operator identity. Basic OpenAI smoke cases should validate success, usage, cost, routing, Langfuse, and session-history invariants rather than brittle exact natural-language output. Keep the prod-cutover failure guards active by default: async task exceptions, ASGI exceptions, `KeyError: choices`, stale `Content-Length` / `h11` protocol failures, upstream passthrough 429/5xx traces, and the OpenAI Responses nested-object-schema regression must fail the run instead of surfacing only as downstream session-history gaps; warning-only optional cases must not mask command timeouts or runtime-log hard failures. Before future prod promotions, add a production-style preflight that validates the exact image / installed wheel path on `:4001` plus a small explicit promotion-gate set for opt-in provider lanes, so packaging and adapter metadata gaps are caught before touching `:4000`.

- Replace the GPT-5.5 local cost-map entry with official API pricing once OpenAI publishes it.
  Current state: OpenAI's latest-model doc says GPT-5.5 is available in ChatGPT and Codex, with API availability coming soon. The local `gpt-5.5` / `chatgpt/gpt-5.5` entries use the rates surfaced by the live Claude CLI `modelUsage` from the validated `:4001` pass-through run so session-history cost tracking remains non-zero in the interim.


## Next

- Decide whether to repair historical Gemini control-plane `session_history` rows.
  The live logging path now skips Gemini Code Assist control-plane calls, so new native Gemini CLI runs no longer create `gemini/unknown` zero-token rows. If historical aggregates must be clean, add a small targeted repair/migration to delete or reclassify the previously logged `loadCodeAssist`, `listExperiments`, `retrieveUserQuota`, and `fetchAdminControls` rows rather than backfilling them to `gemini-2.5-flash`.

- Resolve Claude `/anthropic` reasoning-effort translation for OpenAI first.
  Design target: introduce a shared Anthropic-adapter reasoning-effort normalizer that accepts `output_config.effort`, direct `reasoning_effort`, `thinking.type`, and `thinking.budget_tokens`, then emits both the original requested effort and the translated OpenAI-native value. For OpenAI Responses, write `reasoning={"effort": ...}`; for any chat-completions fallback, write `reasoning_effort` only when the target model supports it. Do not clamp Claude `max` / `xhigh` to `high` when model info says the selected OpenAI model supports `xhigh`; otherwise clamp to `high` and record the clamp reason. Langfuse/session target: metadata should include `reasoning_effort_requested`, `reasoning_effort_source`, `reasoning_effort_native_provider=openai`, `reasoning_effort_native_field`, `reasoning_effort_native_value`, `openai_reasoning_effort`, and clamp metadata when applicable; tags should include `effort:<value>` and `openai-effort:<value>`. Harness target: add OpenAI `/anthropic` adapter cases for `output_config.effort=max/xhigh/high/medium/low` plus budget-derived effort, assert the outbound OpenAI payload has the expected `reasoning.effort` and no raw Anthropic `output_config`, then assert Langfuse observation metadata/tags and `public.session_history.metadata` contain the normalized effort fields while existing reasoning-token fields still populate. Use `.analysis/anthropic-adapter-reasoning-effort-map-2026-04-24.md` as the intended mapping.

- Resolve Claude `/anthropic` reasoning-effort translation for Google/Gemini second.
  Design target: feed the shared normalized effort into the Google completion adapter before it builds Gemini `completion_kwargs`, so Claude `output_config.effort` no longer gets lost on the Google lane. Let existing Gemini translation map normalized effort into `generationConfig.thinkingConfig.thinkingLevel` for Gemini 3+ and `thinkingBudget` for Gemini 2.5, but normalize Claude `max` / `xhigh` to Gemini's highest supported setting and record the clamp. Langfuse/session target: metadata should include the generic normalized effort keys plus `gemini_reasoning_effort`, `gemini_thinking_level`, `gemini_thinking_budget` when present, and `reasoning_effort_clamped_from` for `max` / `xhigh`; tags should include `effort:<native-or-clamped-value>` and `gemini-effort:<value>`. Harness target: add Gemini `/anthropic` adapter cases for `max/xhigh/high/medium/low` and budget-derived effort, assert the outgoing Google request contains the expected `thinkingConfig`, assert unsupported raw Anthropic `output_config` is absent, and assert Langfuse plus `public.session_history.metadata` preserve both requested Claude effort and translated Gemini setting.

- Resolve Claude `/anthropic` reasoning-effort translation for OpenRouter third.
  Design target: make the shared normalizer feed the OpenRouter completion/responses adapter lanes using config-driven capability checks (`supports_reasoning`, `supported_openai_params`, `supports_xhigh_reasoning_effort`, `supports_none_reasoning_effort`) instead of hardcoded model-name checks. Pass `reasoning_effort` only for OpenRouter models that support it; if a model supports reasoning but not effort levels, use the provider's supported reasoning toggle/path and record the effort as degraded rather than silently dropping it. Langfuse/session target: metadata should include the generic normalized effort keys plus `openrouter_reasoning_effort`, `openrouter_reasoning_mode`, `reasoning_effort_clamped_from`, and `reasoning_effort_clamp_reason`; tags should include `openrouter-effort:<value>` when an effort level is sent and `reasoning-effort-clamped` when downgraded. Harness target: add OpenRouter `/anthropic` adapter cases for at least one effort-level-capable model, one reasoning-capable-but-level-limited model, and one unsupported model. Assert outbound payload shape, absence of raw Anthropic `output_config`, Langfuse metadata/tags, and `public.session_history.metadata` for every `max/xhigh/high/medium/low` mapping shape called out in `.analysis/anthropic-adapter-reasoning-effort-map-2026-04-24.md`.

- Resolve Claude `/anthropic` reasoning-effort translation for NVIDIA fourth.
  Design target: treat NVIDIA reasoning support as model-specific. Use the shared normalizer, then consult NVIDIA model/provider capabilities before sending `reasoning_effort`; when a NVIDIA target only supports a boolean thinking/reasoning mode, enable the native mode for non-`none` effort and record that level granularity was lost. Never send unsupported Anthropic `output_config` or unsupported OpenAI-style effort fields to NVIDIA. Langfuse/session target: metadata should include the generic normalized effort keys plus `nvidia_reasoning_effort`, `nvidia_reasoning_mode`, and clamp/degradation reason fields; tags should include `nvidia-effort:<value>` when a native value is sent or `reasoning-effort-clamped` when degraded. Harness target: add NVIDIA `/anthropic` adapter cases for representative DeepSeek, GLM, and MiniMax/NVIDIA targets, validating outbound native payloads, Langfuse observation metadata/tags, and `public.session_history.metadata` for `max/xhigh/high/medium/low`; allow omitted native effort only when metadata explicitly records unsupported/degraded reasoning.

- Design and implement Claude `/anthropic` cache translation for OpenAI first.
  Translation design: treat Anthropic `cache_control: {"type": "ephemeral"}` blocks as explicit cache intent even though OpenAI prompt caching is mostly provider-managed. Preserve the cache signal through the Anthropic-to-OpenAI adapter as LiteLLM metadata and as the most-native OpenAI control surface available for the selected path: keep `prompt_cache_key` / stable request cache identity when provided or derive a deterministic cache identity from the marked Anthropic prefix, and pass no unsupported `cache_control` field to OpenAI chat/responses payloads. For Responses API traffic, map provider usage `input_tokens_details.cached_tokens` / `prompt_tokens_details.cached_tokens` to Anthropic-compatible `cache_read_input_tokens`; when an explicit cache signal was sent but OpenAI returns no cached-token detail, store `provider_cache_attempted=true`, `provider_cache_status=miss`, and `provider_cache_miss_reason=openai_no_cached_tokens_reported` rather than losing the attempt. Langfuse design: usage details on the generation/span must include `cache_read_input_tokens`, `cache_creation_input_tokens` when known, generic `usage_provider_cache_*`, and `openai_provider_cache_*` metadata; trace/session metadata must retain the provider-cache attempt/status fields so backfill and live writes agree. Harness design: add an OpenAI-focused `/anthropic` adapter case that sends a long, repeated Claude-style cached prefix through an OpenAI/Codex target twice, validates the outgoing OpenAI payload has no raw Anthropic `cache_control`, validates Langfuse observation usage/metadata has the cache attempt/read/write fields, and validates `public.session_history` covers every existing cache column: `cache_read_input_tokens`, `cache_creation_input_tokens`, `provider_cache_attempted`, `provider_cache_status`, `provider_cache_miss`, `provider_cache_miss_reason`, `provider_cache_miss_token_count`, and `provider_cache_miss_cost_usd`. Include both streaming and non-streaming Responses paths because cached-token accounting often lands in different final usage objects.

- Design and implement Claude `/anthropic` cache translation for Google/Gemini second.
  Translation design: preserve Anthropic `cache_control` on text blocks long enough for the Gemini context-caching layer to split cached and non-cached messages, create/reuse `cachedContent`, and pass `cachedContent` / `cached_content` to the Gemini request body. Map Anthropic TTL hints to Gemini TTL only when the value is valid for the Gemini cache API; otherwise keep the cache attempt but mark the invalid TTL in metadata. Map Gemini `usageMetadata.cachedContentTokenCount` to `cache_read_input_tokens`, and map cache-write aliases such as `cacheWriteInputTokens`, `cacheWriteInputTokenCount`, and `cacheCreationInputTokens` to `cache_creation_input_tokens`. Langfuse design: retain raw Gemini `usageMetadata`, normalized `usage_cache_*`, generic `usage_provider_cache_*`, and `gemini_provider_cache_*` metadata on observations/traces so live session writes and historical backfill derive the same cache status. Harness design: add Gemini `/anthropic` adapter cases for first-write and second-read behavior using a long cached prefix, assert the cached portion was removed from the final generation request when `cachedContent` is used, assert Langfuse records both raw `usageMetadata` and normalized cache fields, and assert `public.session_history` cache columns are populated with `provider_cache_status=write` on creation and `provider_cache_status=hit` on reuse when the provider reports it; include a miss case for invalid/too-short cached content.

- Design and implement Claude `/anthropic` cache translation for OpenRouter third.
  Translation design: do not limit cache preservation to model names containing `anthropic` or `claude`; instead use OpenRouter provider capability detection for models that accept content-block `cache_control` (`claude`, `gemini`, `minimax`, `glm`, `z-ai`, and any future config-driven additions). For OpenRouter targets that do not support native cache controls, strip raw Anthropic `cache_control` from the upstream payload but preserve `provider_cache_attempted=true` and a deterministic miss reason in metadata. Map OpenRouter usage/cost cache evidence into normalized `cache_read_input_tokens` / `cache_creation_input_tokens` when returned, and keep OpenRouter-specific cache metadata under `openrouter_provider_cache_*`. Langfuse design: trace/session metadata must include the adapter target, OpenRouter routed model, cache support decision, raw provider usage, generic `usage_provider_cache_*`, and `openrouter_provider_cache_*` values. Harness design: add one OpenRouter Claude-compatible case that proves `cache_control` reaches OpenRouter content blocks, one OpenRouter Gemini/GLM-style case that proves non-Claude supported models are not stripped prematurely, and one unsupported OpenRouter model case that proves raw `cache_control` is removed while Langfuse and `public.session_history` still record the attempted miss and all cache columns are covered.

- Design and implement Claude `/anthropic` cache translation for NVIDIA fourth.
  Translation design: assume NVIDIA OpenAI-compatible chat targets do not accept Anthropic `cache_control` unless a specific NVIDIA model advertises native cache support. Strip raw cache-control fields from NVIDIA payloads, preserve the cache attempt and breakpoint summary in LiteLLM metadata, and mark `provider_cache_status=miss` with a clear reason such as `nvidia_no_native_prompt_cache` when no provider cached-token usage is returned. If NVIDIA later exposes cached-token usage, map it into `cache_read_input_tokens`; if it exposes explicit cache writes, map them into `cache_creation_input_tokens`; otherwise leave token counts at zero while preserving the attempted/miss state. Langfuse design: NVIDIA adapter traces must carry `nvidia_provider_cache_*` and generic `usage_provider_cache_*` metadata, plus the final normalized upstream model, so `public.session_history` and Langfuse sessions show the same cache attempt even without provider savings. Harness design: add NVIDIA `/anthropic` adapter validation that sends a cached Claude prefix, asserts the NVIDIA request contains no unsupported Anthropic `cache_control`, asserts Langfuse observation and trace/session metadata record the normalized cache attempt/miss state, and asserts `public.session_history` has complete cache-column coverage with zero read/write tokens allowed only when `provider_cache_attempted=true`, `provider_cache_status=miss`, and `provider_cache_miss_reason` is populated.

- Keep future harness bundle publishes on version `0.0.14` or newer.
  The `0.0.14` harness bundle includes the controlled Claude trace `userId` validation, explicit per-run Claude settings overlay, longer peeromega fanout timeout, the narrow OpenRouter provider-unavailable timeout / command-failure classifier, and the default-suite exclusion for GPT-OSS edge cases needed for real prod validation.

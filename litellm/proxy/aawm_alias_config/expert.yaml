# Temporary litellm-alpha stabilization mapping for ``expert``.
defaults: {}

aliases:
  - name: expert
    candidates:
      - provider: openai
        model: gpt-5.6-terra
        route_family: codex_responses
        priority: 100
        reasoning_effort: max

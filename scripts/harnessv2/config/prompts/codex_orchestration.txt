You are the parent on alias {parent}.

Call spawn_agent now with model={child} and a non-empty message that tells
the child to execute two independent harmless local shell commands in
parallel, not sequentially: `pwd` and `uname -s`. The child must return only
those two stdout strings. Do not run the commands yourself.
Do not spawn any other children.

After the child returns, print that exact stdout on its own line, list the
child alias, then reply with the exact token hv2-codex-child on its own
line. Do not print the token until the child's stdout is visible.

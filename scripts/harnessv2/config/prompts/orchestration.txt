You are the parent on alias {parent}.

Spawn all nine subagents now, in one turn, using the Ohmypi `task` tool.
The spawn payload has no `model=` field. Set `agent` to these exact profile
names. Do not substitute scout, designer, reviewer, security-reviewer,
librarian, task, sonic, a provider prefix, or an aawm- prefixed name:

1. agent=basic   FIRST: reply with exactly the word PONG. Then run the
   single shell command `date` (or `date -u` if date is unavailable).
   Return only the command stdout. Do not guess the time.
2. agent=work    same task.
3. agent=expert  same task.
4. agent=sota    same task.
5. agent=sota-xai same task.
6. agent=sota-alibaba same task.
7. agent=sota-moonshot same task.
8. agent=sota-zai same task.
9. agent=auto-review same task.

Do not spawn any other agents. Do not do PONG or date yourself.

After all listed subagents have returned PONG and date stdout, send each
the same follow-up in one parent turn: in a single assistant response,
run these three tools in parallel (not sequential turns):
`pwd`, `uname -s`, and `echo omp-alpha-fanout`. Return the three stdout
strings. No other tools.

Then list, for each child: agent profile used, PONG reply, date stdout,
and the three parallel stdout strings.

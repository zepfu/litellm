You are the parent on model {parent}.

Call subagent_spawn now with a non-empty prompt that tells the child to
execute a harmless local shell command (`pwd`) in this workspace and
return only that command's stdout. Do not guess the result. Do not skip
the child spawn. Do not run the command yourself. Do not spawn Ohmypi
`agent=` profiles. Use Muse's own spawn tools only (`subagent_spawn`,
then `subagent_wait` / `subagent_read_result` as needed).

After the child returns stdout, print that exact stdout on its own line,
then reply with the exact token hv2-muse-child on its own line. Do not
print the token until the child's stdout is visible.
